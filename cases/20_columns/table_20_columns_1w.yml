db: "test"
table: "table_20_columns_1w"
count: 10000

#if not from s3, path must be relative from ./data
#if from s3, s3 path here
path: "20_columns/10000_20_columns_load_data.csv" 
s3: "false"
terminated: ","
ddl: "create table table_20_columns_1w(
clo1 tinyint,
clo2 smallint,
clo3 int,
clo4 bigint,
clo5 tinyint unsigned,
clo6 smallint unsigned,
clo7 int unsigned,
clo8 bigint unsigned,
col9 float,
col10 double,
col11 varchar(255),
col12 Date,
col13 DateTime,
col14 timestamp,
col15 bool,
col16 decimal(5,2),
col17 text,
col18 varchar(255),
col19 varchar(255),
col20 varchar(255)
);"
